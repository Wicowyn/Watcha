<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/primary</item>
    </style>

    <style name="Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:maxLines">1</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:background">@drawable/button_yellow</item>
    </style>

    <style name="SlidingTabs">
        <item name="pstsShouldExpand">true</item>
        <!--<item name="pstsTabBackground">@drawable/tabs</item>-->
        <item name="pstsIndicatorColor">@color/white_light</item>
        <item name="android:textColor">@color/white</item>
        <!--<item name="pstsUnderlineColor">@color/primary</item>-->
        <item name="pstsIndicatorHeight">4dp</item>
        <item name="android:background">@color/primary</item>
    </style>

</resources>
